#include <stdio.h>

/**
 * main - print all the letters except q and e
 * Return: Always 0
 */
int main(void)
{
	char la;
	
	for (la = 'a'; la <= 'z'; la++)
	{
		if (la !a 'e' && la != 'q')
			putchar(la);
	}

	putchar('\n');

	return (0);
}
